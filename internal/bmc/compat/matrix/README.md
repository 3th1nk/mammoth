# Embedded default matrix

Intentionally empty: entries accumulate with community reports. Override or
extend at runtime with MAMMOTH_COMPAT_DIR (YAML files, format in
docs/compat/README.md). This directory is embedded via go:embed — keep at
least one file here for the glob to resolve.
