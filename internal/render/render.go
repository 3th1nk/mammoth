// Package render hosts answer-file rendering (text/template) over the Install
// Spec × machine layout: storage, network, identity, access, scripts
// (docs/06-install-pipeline.md). Ships with M3.
package render
