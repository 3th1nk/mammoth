// Package inventory hosts the probe implementations: redfish (M1),
// inband_ssh (M2), ramdisk (M5) — see docs/05-inventory.md. The prober facet
// executes the in-band probes in an isolated deployment unit.
package inventory
